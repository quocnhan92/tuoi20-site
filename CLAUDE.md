# Tuoi20 Studio
Static HTML/CSS/JS, no third-party scripts. GitHub Pages via .github/workflows/pages.yml.
OAuth: connect.html → TikTok → callback.html; secret/token exchange is local in private tiktok repo.
Never commit credentials, response codes, or user media. TikTok verification text file is public by design.
